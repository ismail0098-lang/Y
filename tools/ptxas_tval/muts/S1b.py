# no-op barrier AND the SASS store moved across it.  Caught by the SNAPSHOT
# obligation, NOT by the barrier -- which is why it is no evidence for H_k.
exec(open('muts/S1c.py').read()); exec(open('muts/S1.py').read())
