--- llvm_emitter.rs
+++ llvm_emitter.rs
@@ -2135,11 +2135,7 @@
             }
             Expr::MemberAccess { base, member, .. } => {
-                let target_val = if let Expr::UnaryOp { op: UnaryOp::Deref, operand, .. } = &**base {
-                    self.emit_expr(operand, None, None) // Skip the load, keep the pointer
-                } else {
-                    self.emit_expr(base, None, None)
-                };
                 let lval = self.emit_lvalue(expr);
                 let field_ty = self.infer_type(expr);
                 self.emit_load(&lval, &field_ty)
             }
