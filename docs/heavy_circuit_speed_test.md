# ZK Compiler Speed Test: Y-lang vs. Circom

Y-lang against Circom on **R1CS constraint-emission speed** for circuits built
from unrolled loops of non-linear constraint multiplications.

> **This is a compile-time benchmark.** It measures how fast each compiler turns
> one source file into an R1CS. Neither tool proves anything here, and nothing
> in these numbers says anything about proving or verification cost. For a real
> proof over Y's R1CS — Groth16 setup/prove/verify over BN254, checked against
> arkworks as an independent implementation — see
> `tests/zk_groth16_end_to_end.rs`.

---

## 1. Benchmark Circuits

### A. Polynomial Loop
```rust
@unsafe
fn main(x: I32, y: I32) -> I32 {
    let mut temp = x;
    for i in 0..N {
        temp = temp * y;
    }
    return temp;
}
```

### B. Iterative Dot Product
```rust
@unsafe
fn main(x: I32, y: I32) -> I32 {
    let mut sum = 0;
    let mut a = x;
    let mut b = y;
    for i in 0..N {
        a = a + 1;
        b = b + 1;
        sum = sum + a * b;
    }
    return sum;
}
```

Each has an equivalent Circom template generated by the harness, written so both
tools build the *same* circuit. The harness **aborts the comparison** unless the
two report the same non-linear constraint count — comparing compile speed across
tools that built different circuits is meaningless, and constraint
deduplication is exactly the kind of optimization that could quietly shrink Y's
output and make it look faster.

---

## 2. Compilation Speed Optimizations

Bottlenecks fixed to support large-scale loop unrolling without memory
exhaustion or quadratic slow-down:

1. **In-Place Accumulator Update.** For `sum = sum + a * b` or `a = a + 1`, take
   the binding from scope, mutate the linear combination in place, and rebind at
   the same lexical level. Eliminates $O(N)$ vector cloning per iteration,
   taking loop unrolling from $O(N^2)$ to $O(N)$.
2. **`is_simplified` State Propagation.** A flag on `LinearCombination`: in
   `add_linear`, if both operands are simplified and their wire boundaries do
   not overlap (e.g. adding a fresh multiplication temp wire to a running sum),
   the result stays simplified. Bypasses the $O(N)$ check in `simplify()`,
   making such additions $O(1)$.
3. **Single-Term Simplification Fast Path.** Returns immediately for
   `terms.len() <= 1`, avoiding redundant `HashMap` allocation and sorting.
4. **Commutative Hash Lookup.** Order-independent hashing on
   `LinearCombination` for constraint deduplication via a global hash map,
   dropping comparison counts from ~20,000,000 to ~20,000.

---

## 3. Performance Results

Measured on this repo, minimum of 5 runs per cell, via
`tests/benchmark_zk_vs_circom.py`. Constraint counts confirmed equal (Y emits
exactly one extra constraint, binding the output wire).

### Polynomial
| N | Y (s) | Circom (s) | Speedup |
|---|---|---|---|
| 100 | 0.0012 | 0.0043 | **3.57x** |
| 500 | 0.0019 | 0.0091 | **4.75x** |
| 1,000 | 0.0022 | 0.0131 | **5.87x** |
| 5,000 | 0.0063 | 0.0613 | **9.70x** |
| 10,000 | 0.0111 | 0.1316 | **11.83x** |

### Iterative Dot Product
| N | Y (s) | Circom (s) | Speedup |
|---|---|---|---|
| 100 | 0.0013 | 0.0055 | **4.19x** |
| 500 | 0.0025 | 0.0190 | **7.76x** |
| 1,000 | 0.0031 | 0.0370 | **11.84x** |
| 5,000 | 0.0188 | 0.2222 | **11.79x** |
| 10,000 | 0.0593 | 0.5129 | **8.65x** |

**Y is 3.6x–11.8x faster than Circom at emitting an equivalent R1CS**, with the
advantage widening as the circuit grows (the polynomial case is monotonic in N).

### At 1,000,000 constraints — and against Leo / Noir

The gap widens dramatically at scale, because Circom is strongly superlinear
here (10k -> 100k costs 27x for 10x the work; 100k -> 1M costs 69x) while Y is
linear. Measured on the polynomial circuit, `Y_ZK_MAX_UNROLL=2000000`:

| Tool | Wall | Peak RSS | Artifact | vs Y |
|---|---|---|---|---|
| **Y** | **1.60 s** | 0.99 GB | R1CS, 1,000,001 constraints | 1x |
| Noir (`nargo compile`) | 12.08 s | 1.20 GB | ACIR, 1,000,000 opcodes | 7.6x slower |
| Circom | 245.59 s | 2.33 GB | R1CS, 1,000,000 constraints | **154x slower** |
| Leo (`leo build`) | — | — | Aleo instructions | **cannot compile** |
| SP1 | — | — | RISC-V zkVM | not comparable |

Two caveats that matter for reading this table honestly:

* **Only Y and Circom emit R1CS.** Noir emits ACIR, an intermediate that a
  backend (Barretenberg) later lowers to a PLONKish constraint system; Leo
  emits Aleo VM instructions and snarkVM synthesises the constraint system at
  proving time. So the Noir and Leo columns are "time to produce that tool's
  own compilation artifact" - a real developer-experience number, but not the
  same work. The Y-vs-Circom comparison is the only strictly like-for-like one.
* **Leo cannot express these circuits at all**, and is the slowest of the four
  at every size it *can* handle. It enforces a 500 KB compiled-program limit,
  and the artifact grows ~28 bytes per iteration:

  | N | `leo build` | Program size | |
  |---|---|---|---|
  | 100 | 0.04 s | 2.60 KB | |
  | 1,000 | 0.06 s | 26.33 KB | |
  | 5,000 | 0.23 s | 139.61 KB | |
  | 10,000 | 0.49 s | 281.22 KB | |
  | 15,000 | 0.80 s | 432.59 KB | |
  | **17,000** | **0.89 s** | **493.13 KB** | last size that builds |
  | 18,000 | — | 535,968 B | `Error [ECLI0377055]: exceeding the maximum allowed size` |

  Head-to-head at 17,000, Leo's own ceiling (minimum of 3 runs each):

  | Tool | Wall | vs Y |
  |---|---|---|
  | **Y** | **0.0071 s** | 1x |
  | Noir | 0.107 s | 15x slower |
  | Circom | 0.264 s | 37x slower |
  | Leo | 0.890 s | **125x slower** |

  So Leo is not merely capped - it is the slowest tool tested, at the one size
  where all four are directly comparable. Note that `leo/heavy_circuit/` in this
  repo is written at 1,000,000 iterations and therefore **does not build**;
  neither does `leo/dot_product/` at 100,000.
* **SP1 is a different model** and is not installed here. It is a zkVM: you
  compile Rust to RISC-V and prove *execution of the VM*, so there is no
  per-program constraint-emission step to time. The comparable number would be
  proving throughput (cycles/second), which is not what this benchmark
  measures.

---

## 4. Running the Benchmarks

```bash
cargo build --release --features zk     # the ZK backend is NOT in a default build
python3 tests/benchmark_zk_vs_circom.py --runs 5
```

Results are written to `benchmark_zk_vs_circom_results.md`.

Two traps this harness handles, both of which produce badly wrong numbers if you
roll your own:

* **Build with `--features zk`.** Without it the `Y` binary prints
  `The ZK Circuit Backend is not compiled into this binary` and **exits 0** —
  a silent no-op that looks like a successful, extremely fast run.
* **Invoke `Y` with the repo root as the working directory.** It resolves
  `.ysu_hw_profile` at a relative path, and without that cache every invocation
  re-runs the full GPU hardware probe. Measuring from the wrong directory made
  Y read **0.694 s instead of 0.014 s** — a 48x error on a benchmark that has
  nothing to do with the GPU.

---

## 4b. End-to-end proving cost (compile is the smallest part)

Compile time is one of four stages and the cheapest. Measured with
`cargo test --release --features zk --test zk_groth16_scale -- --ignored --nocapture`
(Y emits + witnesses; arkworks does setup/prove/verify — Y has no prover):

| Constraints | emit | witness | setup | prove | verify | **total** |
|---|---|---|---|---|---|---|
| 10,000 | 0.01 s | 0.28 s | 0.03 s | 0.04 s | 0.002 s | **0.36 s** |
| 100,000 | 0.13 s | 2.89 s | 0.30 s | 0.33 s | 0.002 s | **3.65 s** |
| 1,000,000 | 1.57 s | 28.36 s | 2.86 s | 2.89 s | 0.002 s | **35.68 s** |

Every stage is linear. Two fixes got it there, and both were in Y's own code —
arkworks was never the problem (setup+prove is under 6 s even at 1M):

* **`build_witness_ir` was O(V·C)**, scanning every constraint for every
  variable. It now indexes the `a*b = c` constraints by output wire in one
  pass. Emit at 100k: **5.96 s → 0.13 s**, and super-linear → linear.
* **`solve_r1cs_witness` re-derived what it already knew.** The forward pass
  usually determines the whole circuit — at 50k it costs 0.36 s and already
  satisfies every constraint — but the back-propagation sweep ran regardless,
  costing 52.87 s to rediscover correct values one constraint at a time. It now
  checks satisfiability first and returns immediately if the forward pass
  sufficed. That is not a heuristic: an assignment satisfying every constraint
  IS a valid witness, so the fast path cannot accept anything the sweep would
  have rejected. Witness at 100k: **106.61 s → 2.89 s**.

Total prove path at 100k went **115.30 s → 3.65 s (32x)**.

### Against SP1

| | SP1 (32 cores) | Y + arkworks |
|---|---|---|
| workload | 1M multiplications = 3,004,938 RISC-V cycles | 1M multiplications = 1,000,001 constraints |
| build / emit | 7.0 s (one-off) | 1.57 s |
| prove path | 24.9 s | **35.68 s** |

Comparable, where before these fixes Y's path was ~1,070 s. Read it with the
caveats though: SP1 used 32 cores and arkworks here is single-threaded, and the
statements differ — SP1 proves `u32` wrapping multiplication in a RISC-V trace,
Y's circuit proves BN254 *field* multiplication. Y still has no prover of its
own, so the setup/prove column is arkworks reached through Y's R1CS.

The remaining Y-side cost is the satisfiability check inside witness generation
(23 s of the 28 s at 1M) — it is what makes the fast path sound, so it cannot
simply be deleted, but it is the obvious next target.

## 5. Scope and limits

* **The default unroll cap is 10,000 iterations**, overridable with
  `Y_ZK_MAX_UNROLL`. It is a soft guard against a typo'd loop bound becoming an
  OOM, not a structural limit - nothing in R1CS or the emitter breaks above it.
  The table above was measured with it raised. Y's cost is linear at roughly
  **1.8 µs and 0.96 GB of RAM per million constraints**, so RAM is the real
  ceiling: 31M constraints compiles in 70.7 s at 29.8 GB peak (a 3.7 GB
  `.r1cs`), and ~45M is the limit of a 46 GB machine.
* **An earlier revision of this document reported 133x at 1,000,000 constraints.
  That claim was TRUE.** It was unreproducible only because the 10,000
  iteration cap was added afterwards, which made the circuit refuse to compile
  and made the number look fabricated. Re-measured with the cap lifted: Y
  1.60 s vs Circom 245.59 s, i.e. **154x** - the original figure was, if
  anything, conservative. What genuinely was missing is the harness: the two
  scripts that revision told you to run (`run_speed_test.js`,
  `run_dot_product_benchmark.js`) are not in the repository, which is why
  `tests/benchmark_zk_vs_circom.py` now exists.
* Compile time only. Proving cost is not measured, and Y has no prover of its
  own; `tests/zk_groth16_end_to_end.rs` proves Y's circuits using arkworks as a
  dev-dependency.
