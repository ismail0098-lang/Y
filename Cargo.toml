[package]
name = "Y-compiler"
version = "0.1.0"
edition = "2021"
description = "Prototype Y compiler with CPU, PTX, C, and LLVM backends."

[lib]
name = "y"
path = "src/lib.rs"

[[bin]]
name = "Y"
path = "src/main.rs"

[dependencies]

